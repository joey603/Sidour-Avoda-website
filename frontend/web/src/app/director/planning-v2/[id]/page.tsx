"use client";

/**
 * Alias historique → URL canonique `/director/planning/[id]` (Planning v2).
 * Conservé pour les anciens liens / favoris.
 * @see `app/director/planning/[id]/page.tsx`
 */
export { default } from "@/app/director/planning/[id]/page";
