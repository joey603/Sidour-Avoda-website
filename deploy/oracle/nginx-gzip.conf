# gzip on est déjà dans nginx.conf ; sans gzip_types, seul text/html est compressé.
gzip_vary on;
gzip_proxied any;
gzip_comp_level 5;
gzip_min_length 256;
gzip_types
    application/json
    application/javascript
    text/plain
    text/css
    application/xml
    application/xhtml+xml;
